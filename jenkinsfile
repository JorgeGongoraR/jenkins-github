pipeline {
  agent any
  
  options {
    timeout(time: 1, unit: 'SECONDS')
  }

  stages {
    stage('Install dependencies') {
      steps {
        sh 'echo "Hello"'
      }
    }
    stage('Run tests') {
      steps {
        sh 'echo "World"'
      }
    }
  }
}
