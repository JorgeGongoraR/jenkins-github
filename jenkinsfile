pipeline {
  agent any
  
  options {
    timeout(time: 10, unit: 'SECONDS')
  }

  stages {
    stage('Install dependencies') {
      steps {
        sh 'sleep 30'
        sh 'echo "Hello"'
      }
    }
    stage('Run tests') {
      steps {
        sh 'echo "World"'
      }
    }
  }
}
