pipeline {
  agent any
  
  options {
    timeout(time: 2, unit: 'MINUTES')
  }

  stages {
    stage('Install dependencies') {
      steps {
        sh 'echo "Hello"'
      }
    }
    stage('Run tests') {
      steps {
        sh 'echo "World"'
      }
    }
  }
}
